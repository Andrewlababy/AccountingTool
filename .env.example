# Qianwen OCR API Configuration
# To enable Qianwen OCR, set your Dashscope API key
# REACT_APP_DASHSCOPE_API_KEY=sk-your-api-key-here

# Note: This key will be included in the client-side bundle
# Only use keys that are safe for client-side exposure
# Consider implementing a backend proxy for production use